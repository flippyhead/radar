# Radar Review UI — Follow-ups

**Context:** Shipped in radar@4.1.0 — `plugins/radar/bin/review-server/` replaces the typed-command `/radar-review` workflow with a tiny local node:http server + single-file vanilla-JS client. The following items were deferred from that PR.

---

## 1. Brain cleanup (separate repo, separate PR)

**Repo:** `/Users/peterbrown/Development/ai-brain` (not this repo)

**Why:** The `/workflow-analyst` slash command that populated Brain's insights/reports tables is gone. The Brain `/insights` page and its Convex backend are now dead code. Radar stands alone; Brain is general-purpose memory only.

**What to delete:**

- `apps/web/src/app/(authenticated)/insights/page.tsx`
- `apps/web/src/features/insights/` (including `InsightCard.tsx` — the visual patterns have been ported to Radar's `index.html`)
- Any nav entry pointing at `/insights`
- Convex tables and functions backing reports/insights:
  - `reports` table + `convex/models/reports/` mutations/queries (`getLatestReport`, `listInsightsByReport`, `listUnresolvedInsights`, `listReports`, `listAllInsights`, `clearAllInsightsAndReports`)
  - Any `insights` table/schema if separate from `reports`
- Convex schema migration to drop the above tables
- Any workflow-analyst-specific MCP tools in the ai-brain MCP server if still present

**What to keep:** everything else in Brain — thoughts, lists, list items, pinned priorities, general memory tools.

**Verification:**
- `grep -r "insights\|reports\|workflow-analyst" apps/web/src convex/ | grep -v node_modules` should return nothing after cleanup.
- `/brain-init` and `/brain-sync` skills in this repo do not reference insights/reports — already clean.

---

## 2. Optional: re-scan / re-recommend buttons in the review UI

**Status:** Deferred. User said "Idk about the buttons" when proposing these.

**Why it might be worth adding:** Right now the review UI is read-only against the existing catalogue. To refresh, the user has to exit, run `/radar-scan` or `/radar-recommend` in Claude Code, then relaunch the UI. A button in the UI header could trigger a fresh scan/recommend without leaving the browser.

**Why it's non-trivial:**

- The scan and recommend workflows are *skills*, not standalone scripts. They involve WebFetch/WebSearch, catalogue enrichment logic, and LLM judgment calls (category assignment, scoring). There is no pure Node entry point that can be invoked from the server.
- Two options:
  1. **Dumb trigger + return to Claude Code:** Button writes a sentinel file (`~/.claude/radar/.refresh-requested`) and exits the server. The `radar-review` skill checks for the sentinel on exit and chains into `/radar-scan` or `/radar-recommend` before reporting the session summary.
  2. **Split the logic:** Extract the non-LLM parts of scan (fetching HN/GitHub/Anthropic feeds, dedup, enrichment heuristics) into the bundled workflow-analyzer CLI, and have the server shell out. This loses LLM-judged categorization and scoring — probably not worth it.

**Recommendation:** If we do this, go with option 1. Keep server + UI pure; let the skill orchestrate the refresh cycle.

**Open question before building:** is the "exit UI → rerun skill → user clicks URL again" loop acceptable, or do we want to keep the same browser tab open and have it auto-reload when the new catalogue lands? The second is more work (server needs to stay alive across scan runs, and the scan has to be invokable without the LLM in the loop).

---

## 3. Possible follow-ups (not yet committed to)

- **CSP headers** on the HTML response. Currently no CSP. Risk is low (localhost-only, no user-supplied HTML beyond what we wrote), but tightening is cheap.
- **Port conflict handling:** server uses `listen(0)` so the OS assigns. No issue unless firewall/system-level interference, in which case the error surfaces via stdout.
- **Multi-tab session counting:** if the user opens the same URL in two tabs, both report to the same session. Not a bug but worth noting if we later add per-tab state.
- **Insights view:** the review UI currently only renders `CATALOGUE.items`. The `insights` array generated by `/radar-recommend` is not displayed. The old text skill had `--insights` — we dropped it. If users want to browse insights, add a second top-level tab in the header (like Brain's Latest/Unresolved pattern).
